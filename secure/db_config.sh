# db_config.sh

# Database configuration
DB_NAME="your_database_name"        # Name of the database to backup
DB_USER="your_database_username"    # Database username
DB_PASSWORD="your_database_password"  # Database password
DB_HOST="localhost"                 # Database host (use 'localhost' if hosted on the same server)
